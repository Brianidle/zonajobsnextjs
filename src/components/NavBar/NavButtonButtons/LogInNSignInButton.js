import LogInButton from "../../Buttons/LogInButton";

const LogInNSignInButton = ({ className }) => {
  return <LogInButton className={className}>INGRESAR</LogInButton>;
};

export default LogInNSignInButton;
