import OptionButton from "../../Buttons/OptionButton";

const ExecutivePositions = ({ className }) => {
  return <OptionButton className={className}>PUESTOS EJECUTIVOS</OptionButton>;
};

export default ExecutivePositions;
