import OptionButton from "../../Buttons/OptionButton";

const NewsBlogButton = ({ className }) => {
  return <OptionButton className={className}>BLOG DE NOTICIAS</OptionButton>;
};

export default NewsBlogButton;
