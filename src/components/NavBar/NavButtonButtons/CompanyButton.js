import OptionButton from "../../Buttons/OptionButton";

const CompanyButton = ({ className }) => {
  return <OptionButton className={className}>SOY EMPRESA</OptionButton>;
};

export default CompanyButton;
